#include main.h


//The print rev recursion trying and winning it

/*int_rev_recursion.c*/


void _print_rev_recursion(char* s)
{
    if (*s == '\0')
        return;
    else
    {
        _print_rev_recursion(a + 1);
    }
}

